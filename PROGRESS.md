# 개발 현황

> 작업 이어받을 때 이 문서부터 읽는다. 마지막 갱신 2026-08-19 09:45

## 제출 일정

| 일자 | 제출물 | 상태 |
|---|---|---|
| 2026-08-19 17:59 | 프로젝트 기술서(.pdf) · DB ERD(.dbml) · API(.yaml) | **산출물 생성 완료** — 문서 최신화만 남음 |
| 2026-08-20 09:59 | 발표 자료(.pdf) | 생성 완료 — 문서 최신화만 남음 |

## 실행

```bash
docker-compose up -d                      # PostgreSQL 18 (5433)
cd backend && ./gradlew bootRun           # 8080
cd frontend && npm run dev                # 5173
```

문서 재생성

```bash
python3 tools/build_wireframes.py   # 와이어프레임 HTML → PNG (dev 서버 필요)
python3 tools/build_pdf.py          # 기술서 PDF
python3 tools/build_slides.py       # 발표 자료 PDF
```

## 구현 완료

| 영역 | 내용 |
|---|---|
| 회원 | 이메일 가입·로그인 (JWT), 중복 검증 |
| 경기 | 일정·결과 조회, **팀 순위 자체 산출**(승률·승차), 날씨 조회 |
| 구장 | 목록·상세, **구역별 만족도 집계**, 구역 후기 |
| 직관 기록 | 작성·목록, 중복 방지(409), 사진 업로드(EXIF 위치 제거·썸네일) |
| 전적 | 통산·구장별·상대팀별·요일별, **소표본 정책**, 연승 |
| 동행 | 모집·지원·**선착순 확정(동시성 3중 방어)**, 취소 |
| 동행 소통 | 공개 댓글, **확정자 전용 대화방**(증분 조회·읽음·읽기전용 전환) |
| 안전 | 신고(모집글/댓글/메시지/사용자), 차단 |
| 관람 계획 | **제약조건 자동 편성**, 제약별 제외 집계, 완화 후보 제시 |
| 날씨 | 기상청 단기예보 연동(배치 동기화), 격자 변환, 기록 시점 날씨 보관 |
| 기록 상세 | 사진 뷰어·개별 삭제, 기록 삭제(전적·구역집계 차감) |
| 배지 | 6종 자동 판정(첫직관·첫승리·원정개시·전구장·3연승·시즌10경기), 조회 시 재평가 |
| 알림 | 배지 획득·동행 확정·결과 정정 시 자동 생성, 미읽음 배지, 전체 읽음 |

**화면 18개**: 홈(A/B 분기) · 로그인 · 회원가입 · 경기 · 팀순위 · 기록작성 · 기록목록 ·
**기록상세** · 전적 · 구장목록 · 구장상세 · 동행목록 · 동행상세 · 동행대화방 ·
관람계획 · 가이드 · 마이페이지 · **알림**

**디자인**
- **구단 시그니처 컬러**: `src/lib/team.js` + `TeamMark.vue` 로 10개 구단 색 통일 적용.
  패배 팀은 흐리게 처리해 승패가 먼저 읽히게 한다
- 색을 데이터에 사용 (승 파랑 / 패 빨강 / 표본부족 회색), 승률 막대, 강조 카드(그라데이션)
- **데스크톱(900px~): 좌측 고정 사이드바 + 본문 최대 1400px 3단 그리드**
- 모바일(~900px): 하단 탭 + 단일 컬럼 (모바일 웹 작업 시 이 기반을 확장한다)
- 라우트 전환 페이드, 스크롤 최상단 복귀, 빈 상태 아이콘
- 로그인·회원가입·소셜 콜백 화면은 사이드바 없이 가운데 한 단(`.app.auth`)

**소셜 로그인 (REQ-F-003)**

구글 · 네이버 · 카카오를 Authorization Code 방식으로 붙였다. 코드 교환에 클라이언트
시크릿이 필요해 교환·프로필 조회는 전부 서버에서 한다.

- 계정 식별은 이메일이 아니라 `(provider, provider_user_id)`. 카카오는 이메일 제공이
  비즈앱 심사 대상이라 내려오지 않을 수 있고, 이메일은 제공자 쪽에서 바뀔 수도 있다.
- 이메일이 함께 오고 그 이메일로 가입한 계정이 이미 있으면 그 계정에 연동한다.
- 제공자 닉네임이 이미 쓰이고 있으면 뒤에 숫자를 붙여 유일하게 만든다.
- **키를 넣지 않은 제공자는 `/auth/oauth/providers` 에서 빠지고 화면에도 버튼이 뜨지 않는다.**
  세 곳 다 발급받지 않은 상태에서도 앱이 그대로 돌아간다.
- state 는 서버가 만들어 내려주고 콜백 화면이 sessionStorage 값과 대조한다.

키를 넣고 띄우려면

```bash
JIKGWAN_GOOGLE_CLIENT_ID=... JIKGWAN_GOOGLE_CLIENT_SECRET=... \
JIKGWAN_NAVER_CLIENT_ID=...  JIKGWAN_NAVER_CLIENT_SECRET=... \
JIKGWAN_KAKAO_CLIENT_ID=...  ./gradlew bootRun
```

각 제공자 콘솔에 리다이렉트 URI `http://localhost:5173/oauth/{google|naver|kakao}` 를 등록해야 한다.

| 제공자 | 키 발급 | 이메일 수집 |
|---|---|---|
| 구글 | 즉시 | 기본 제공 |
| 네이버 | 즉시 | 즉시 사용 가능 |
| 카카오 | 즉시 | 비즈앱 전환 심사 필요 (없어도 로그인은 됨) |

**운영자 화면 (SCR-ADM-001, REQ-F-601 ~ 604 · 607)**

- 제보 검토: 이미 시작했는데 결과가 확정되지 않은 경기를 스코어별 제보 집계와 함께 보여준다.
  최다 일치 제보를 폼 기본값으로 채워 둔다.
- 정정: 사유 필수, 변경 전후 값을 `game_revisions` 에 **추가 전용**으로 남긴다.
  정정하면 그 경기를 기록한 모든 사용자의 전적을 재계산하고 알림을 보낸다.
- 종료(FINISHED)로 바꿀 때만 스코어를 요구하고, 취소·서스펜디드는 스코어를 비운다.
  전적에서 빠져야 하기 때문이다.
- 경기 등록: 홈=원정 거부, 같은 날 같은 구장 중복 거부.
- 권한 없는 사용자는 사이드바에 메뉴가 없고, URL 로 들어와도 잠금 화면만 보인다.

**구장 · 좌석 구역 관리 (REQ-F-605)**

구역은 **지우지 않고 비활성화만 한다.** 구역은 관람 기록이 참조하고 구역별 만족도
집계의 단위이기도 해서, 지우면 과거 기록의 좌석 정보와 그 구역에 쌓인 평가가 함께
사라진다. 더 쓰지 않을 구역은 비활성으로 돌려 새 기록에서만 빠지게 한다.

- 운영자 목록에는 비활성 구역도 나온다. 되살릴 수 있어야 하기 때문이다.
- 일반 조회(`GET /stadiums/{id}`)에서는 빠지고, id 를 직접 보내도 `ZONE_INACTIVE` 로 막는다.
- 구역별 관람 기록 건수를 함께 보여준다. 비활성화 전에 영향 범위를 보라는 뜻이다.
- 구장 자체는 삭제 없이 정보 수정만 제공한다 (주소 · 수용 인원 · 기상청 격자).

**운영자 계정 지정**

DB 를 직접 고치지 않도록 환경변수로 지정한다. **이미 가입한 계정만** 승격하며 계정을
새로 만들지는 않는다. 목록에서 빠져도 강등하지 않는다.

```bash
JIKGWAN_ADMIN_EMAILS=admin@example.com ./gradlew bootRun
```

**응원팀 설정**
- `GET /teams`, `GET·PATCH /users/me` 추가
- 회원가입 화면과 마이페이지에서 구단 마크로 고른다 (`TeamPicker`)
- 응원팀을 바꿔도 이미 남긴 기록의 승패는 소급하지 않는다

**검증한 것**
- 동행 선착순: 30명 동시 요청 → 정확히 정원만 확정, 순번 중복·누락 없음
- 사진 업로드: GPS 4태그 → 0태그, 썸네일 400px
- 소표본 정책: 표본 5경기 미만 승률 미산출 (API `winRate: null` + `smallSample: true`)
- 팀 순위: 630경기에서 승차 계산 정확
- 운영자 정정: 우천 노게임 처리 시 8경기 5승3패 → 7경기 5승2패(중립 1), 이력·알림 생성 확인.
  이후 원복하여 데모 데이터는 그대로. 사유 누락 400, 종료인데 스코어 없음 400,
  일반 회원 접근 403
- 구역 비활성화: 일반 구장 상세에서 사라지고 되살리면 다시 나옴. 비활성 구역 id 로
  기록 작성 시 ZONE_INACTIVE 400. 중복 이름 409, 빈 이름 400
- 소셜 로그인: 세 제공자 인가 URL 이 각 규격대로 생성됨, 키 없는 제공자는 400 + 목록에서 제외,
  잘못된 code 는 401. 실제 로그인은 키 발급 후 확인 필요

## 남은 일

### 1순위 — 문서 최신화 (마감 전 필수)
구현하면서 추가된 것을 문서에 반영해야 한다.

- [ ] API 명세: 대화방·댓글·신고·차단·순위·날씨 엔드포인트 반영 (일부만 반영됨)
- [ ] API 명세: 소셜 로그인(`/auth/oauth/*`), `/teams`, `/users/me`,
      운영자 구역 관리(`/admin/stadiums/*`, `/admin/zones/*`) 추가
- [ ] 요구사항 정의서: 구현 상태 반영, 새로 확정된 값 기재
- [ ] 데이터 모델링: V5·V6·V7 마이그레이션 반영 (game_weather, companion_*, reports, user_blocks)
- [ ] 화면 설계서: 신규 화면 목록 추가 (소셜 콜백 SCR-AUTH-003 포함)
- [ ] 발표 자료: 구현 범위 갱신
- [ ] `build_pdf.py` · `build_slides.py` 재실행

### 2순위 — 모바일 웹
데스크톱을 먼저 완성하고 모바일 웹을 별도로 다듬기로 했다(사용자 방침).
현재 모바일은 동작하지만 데스크톱만큼 손보지 않았다.
- 900px 미만에서 하단 탭 + 단일 컬럼으로 이미 동작
- 터치 타깃 크기, 가로 스크롤 영역, 사진 뷰어 제스처 등 점검 필요

### 3순위 — 미구현 기능
- 티켓 카드 공유 (REQ-F-704)
- 기록 수정 화면 (API 는 있으나 화면 없음)
- 소셜 로그인 실제 키 발급 후 종단 확인 (코드는 완료)

## 주의사항

- **DB를 새로 만들면 기록 데이터가 사라진다.** 시드(구장·구단·720경기)는 Flyway가 복원하지만
  사용자 기록은 API로 다시 넣어야 한다.
- 기상청 인증키는 환경변수 `JIKGWAN_KMA_SERVICE_KEY` 로 주입한다. 소스에 넣지 않는다.
- 소셜 로그인 키도 마찬가지로 환경변수다. `application.yml` 에 값을 직접 쓰지 않는다.
- `user_social_accounts` 는 V1 에 이미 있었다. V7 은 CREATE 가 아니라 ALTER 다.
  새 테이블을 만들기 전에 V1 스키마를 먼저 확인한다.
- 구단 공식 채널 URL(`teams.homepage_url` 등)은 비어 있다. 실제 주소 확인 후 입력해야 한다.
- 외부 데이터 출처는 요구사항 정의서 7장에 근거와 함께 정리되어 있다. 임의로 바꾸지 않는다.
- Vue SFC 수정 시 `</template>` 문자열 치환에 주의한다. 안쪽 `<template v-if>` 닫는 태그에
  걸려 `<style>` 이 템플릿 내부로 들어간 사고가 있었다.
- 배지는 기록 저장 시점과 조회 시점 양쪽에서 평가한다(멱등).
- `@Transactional` 은 같은 빈 안에서 자기 호출하면 걸리지 않는다. `AdminBootstrap` 을
  `ApplicationRunner` 로 구현해 컨테이너가 프록시를 거쳐 부르도록 한 이유다.
- 검증하며 만든 데이터가 남아 있다: 8/18 SSG@NC(id 631) 5:3 확정,
  9/1 잠실 NC vs LG(id 721) 등록, 경기 593 정정 이력 2건, 잠실 "테스트 구역"(비활성).
  관람 기록에는 영향이 없다. 구역은 설계상 삭제 API 가 없으므로 지우려면 DB 에서 지운다.
